1) The bug was that num1 and num2 are set as strings instead of integers, so the two strings were concatenated together when adding them.  
2) Use the unary plus operator to convert the strings to numbers. 
